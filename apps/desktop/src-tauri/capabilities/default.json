{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the desktop app: invoke our own commands, open dialogs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
